apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "social-game.fullname" . }}-test-connection"
  labels:
    {{- include "social-game.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "social-game.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
